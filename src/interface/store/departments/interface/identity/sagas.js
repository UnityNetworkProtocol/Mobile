/* --- Global Modules --- */
import idx from 'idx';
import { all, put, takeEvery } from 'redux-saga/effects';

/* --- Local Modules --- */
import actions from './actions'


export default function* SagaAll() {
  yield all([

  ]);
}